# yandeDL-go
go version yandeDL from my python version
```
      ~/git/yandeDL-go  on   main ?1  ./yandeDL-go                                                                                                       ✔  with wudaown@dev    at 03:31:16 AM 
  Usage of ./yandeDL-go:
    -c concurrent
          concurrent task number. (default 1)
    -d directory
          directory to keep the images. Defaults to `pwd`/`tag`
    -t tag
          tag to search for
```
